# Frontend is deployed automatically by Vercel on every push to main.
# This workflow is disabled — no EC2 deployment needed for the frontend.
name: Deploy Frontend (disabled — Vercel handles this)

on:
  workflow_dispatch: # only runs if triggered manually

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Frontend deploys automatically via Vercel. Nothing to do here."
